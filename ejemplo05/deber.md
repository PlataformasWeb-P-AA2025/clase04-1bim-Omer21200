Diseñar una estructura de datos y un flujo de trabajo donde:

Se tenga una base de datos en CouchDB llama tenis001 que contenga el archivo JSON completo como un solo documento. Esta base sirve como respaldo o fuente original de los datos.

Se tenga otra base de datos llamada tenis002, donde los datos del archivo JSON sean procesados e insertados uno por uno.